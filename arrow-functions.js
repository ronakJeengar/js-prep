const greetUser = (name) => console.log(`Hello ${name}`);
greetUser("Ronak");

const multiplyNumbers = (a, b) => a * b;
console.log(`Multiplication is : ${multiplyNumbers(3, 4)}`);

const isEven = (a) => a % 2 === 0;
console.log(`Is Even : ${isEven(3)}`);
